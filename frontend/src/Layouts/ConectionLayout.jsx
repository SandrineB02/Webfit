import React from "react";

const ConectionLayout = ({ children }) => {
  return (
    <div className="conection-layout">
      <main>{children}</main>
    </div>
  );
};

export default ConectionLayout;
